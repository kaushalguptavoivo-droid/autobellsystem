<Application x:Class="AutoBellSystem.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             ShutdownMode="OnExplicitShutdown"
             Startup="Application_Startup"
             Exit="Application_Exit">
    <Application.Resources>
        <!-- Taskbar Icon template -->
        <tb:TaskbarIcon x:Key="NotifyIcon"
                        IconSource="pack://application:,,,/icon.ico"
                        ToolTipText="School Auto Bell System"
                        TrayMouseDoubleClick="NotifyIcon_DoubleClick">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Open Dashboard" Click="MenuItem_Dashboard_Click" FontWeight="Bold" />
                    <Separator />
                    <MenuItem x:Name="PauseMenuItem" Header="Pause Schedule" Click="MenuItem_Pause_Click" />
                    <MenuItem Header="Test Bell Now" Click="MenuItem_TestBell_Click" />
                    <Separator />
                    <MenuItem Header="Exit" Click="MenuItem_Exit_Click" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Application.Resources>
</Application>
